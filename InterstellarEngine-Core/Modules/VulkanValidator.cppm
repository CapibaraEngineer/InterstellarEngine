export module VulkanValidator;

import<iostream>;

//this thing is to put all the vulkan validation layers in one single place so its more organized

export namespace insterstellarEngineCore{
	class VulkanValidator {
	public:
		void func() {
			std::cerr << "does this even compile";
		}
	};
}